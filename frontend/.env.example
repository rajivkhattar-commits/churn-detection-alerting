# Opt-in hardened production UI (hides dev-only / demo controls at build time).
# Default: unset or false — full toolbar (presets + slider with dev tags), dev venue actions, etc.
# VITE_STRICT_PROD=true
